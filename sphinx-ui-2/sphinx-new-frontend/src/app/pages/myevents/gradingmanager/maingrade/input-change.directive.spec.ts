import { InputChangeDirective } from './input-change.directive';

describe('InputChangeDirective', () => {
  it('should create an instance', () => {
    // const directive = new InputChangeDirective();
    // expect(directive).toBeTruthy();
  });
});
